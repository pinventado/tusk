# TUSK Image Builder

This repository builds and publishes the CSUF TUSK-based Ubuntu teaching image.

The repo is intentionally small and keeps only the files needed to define the package set, bootstrap the image, and publish it to GitHub Container Registry.
